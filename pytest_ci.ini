# -- FILE: pytest.ini (or tox.ini)
[pytest]
;DJANGO_SETTINGS_MODULE = content_aggregator.settings.ci
DJANGO_SETTINGS_MODULE = content_aggregator.settings.floop

# -- recommended but optional:
python_files = tests.py test_*.py *_tests.py
